#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cooking/bin/cooking" "$@"
  ret=$?
else 
  node  "$basedir/../cooking/bin/cooking" "$@"
  ret=$?
fi
exit $ret
